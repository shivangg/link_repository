window.onload = initPage;function initPage() {  document.getElementById("username").onblur = checkUsername;  document.getElementById("email").onblur = checkEmail;  document.getElementById("mobile").onblur = checkMobile;  document.getElementById("submit").disabled = true;}function checkUsername() {  document.getElementById("username").className = "field thinking";  request = createRequest();  if (request == null)    alert("Unable to create request");  else {    var theName = document.getElementById("username").value;    var username = escape(theName);    var url= "checkName.php?username=" + username;    request.onreadystatechange = showUsernameStatus;    request.open("GET", url, true);    request.send(null);  }}function showUsernameStatus() {    if (request.readyState == 4) {   if (request.status == 200) {     // alert("inside showUsernameStatus()");     // alert(request.responseText);      var ok="okay";      var den="denied";      var text=request.responseText;      if (text.indexOf("okay") > -1)     //check if okay is echoed      {      //  alert("okay echoed");        document.getElementById("username").className = "field approved";        document.getElementById("submit").disabled = false;      }      else //+if((text).localeCompare(den)==0)      {       // alert("denied echoed");        document.getElementById("username").className = "field denied";        document.getElementById("username").focus();        document.getElementById("username").select();        document.getElementById("submit").disabled = true;      }      //alert(text);    }  }}function checkMobile() {  document.getElementById("mobile").className = "field thinking";  request = createRequest();  if (request == null)    alert("Unable to create request");  else {    var theName = document.getElementById("mobile").value;    var username = escape(theName);    var url= "checkMobile.php?mobile=" + username;    request.onreadystatechange = showMobileStatus;    request.open("GET", url, true);    request.send(null);  }}function showMobileStatus() {    if (request.readyState == 4) {   if (request.status == 200) {     // alert("inside showUsernameStatus()");     // alert(request.responseText);      var ok="okay";      var den="denied";      var text=request.responseText;      if (text.indexOf("okay") > -1)     //check if okay is echoed      {     //   alert("okay echoed");        document.getElementById("mobile").className = "field approved";        document.getElementById("submit").disabled = false;      }      else //+if((text).localeCompare(den)==0)      {     //   alert("denied echoed");        document.getElementById("mobile").className = "field denied";        document.getElementById("mobile").focus();        document.getElementById("mobile").select();        document.getElementById("submit").disabled = true;      }    //  alert(text);    }  }}function checkEmail() {  document.getElementById("email").className = "field thinking";  request = createRequest();  if (request == null)   alert("Unable to create request");  else {    var theName = document.getElementById("email").value;    var username = escape(theName);    var url= "checkEmail.php?email=" + username;    request.onreadystatechange = showEmailStatus;    request.open("GET", url, true);    request.send(null);  }}function showEmailStatus() {    if (request.readyState == 4) {   if (request.status == 200) {   //   alert("inside showEmailStatus()");   //   alert(request.responseText);      var ok="okay";      var den="denied";      var text=request.responseText;      if (text.indexOf("okay") > -1)     //check if okay is echoed      {   //     alert("okay echoed");        document.getElementById("email").className = "field approved";        document.getElementById("submit").disabled = false;      }      else //+if((text).localeCompare(den)==0)      {    //    alert("denied echoed");        document.getElementById("email").className = "field denied";        document.getElementById("email").focus();        document.getElementById("email").select();        document.getElementById("submit").disabled = true;      }    //  alert(text);    }  }}